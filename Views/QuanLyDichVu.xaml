<Window x:Class="LePhungHa_8991.View.QuanLyDichVu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LePhungHa_8991.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Quản Lý Dịch Vụ" Height="650" Width="1100">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="QUẢN LÝ DỊCH VỤ" FontSize="28" FontWeight="Bold" 
                   Foreground="#9C27B0" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Label Content="Mã Dịch Vụ:" FontWeight="Bold"/>
                    <TextBox x:Name="txt_MaDichVu" Height="35" Padding="5" FontSize="14"/>
                    
                    <Label Content="Tên Dịch Vụ:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBox x:Name="txt_TenDichVu" Height="35" Padding="5" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0">
                    <Label Content="Đơn Giá:" FontWeight="Bold"/>
                    <TextBox x:Name="txt_DonGia" Height="35" Padding="5" FontSize="14"/>
                    
                    <Label Content="Đơn Vị Tính:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBox x:Name="txt_DonViTinh" Height="35" Padding="5" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="10,0,0,0">
                    <Label Content="Mô Tả:" FontWeight="Bold"/>
                    <TextBox x:Name="txt_MoTa" Height="80" Padding="5" FontSize="14" 
                            TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Margin="0,10,0,0">
                    <CheckBox x:Name="chk_TrangThai" Content="Đang cung cấp" 
                             FontSize="14" IsChecked="True"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" 
                           HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Thêm" Click="Them_Click" Width="100" Height="40" 
                           Margin="5" Background="#4CAF50" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Sửa" Click="Sua_Click" Width="100" Height="40" 
                           Margin="5" Background="#2196F3" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Xóa" Click="Xoa_Click" Width="100" Height="40" 
                           Margin="5" Background="#F44336" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Làm Mới" Click="LamMoi_Click" Width="100" Height="40" 
                           Margin="5" Background="#FF9800" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txt_TimKiem" Width="300" Height="35" 
                        Padding="5" FontSize="14" Text="Tìm kiếm dịch vụ..."/>
                <Button Content="🔍 Tìm Kiếm" Click="TimKiem_Click" Width="120" Height="35" 
                       Margin="10,0,0,0" Background="#9C27B0" Foreground="White" 
                       FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>

            <DataGrid x:Name="dgv_DichVu" Grid.Row="1" 
                     AutoGenerateColumns="False" 
                     IsReadOnly="True"
                     SelectionMode="Single"
                     SelectionChanged="dgv_DichVu_SelectionChanged"
                     CanUserAddRows="False"
                     Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã DV" Binding="{Binding MaDichVu}" Width="100"/>
                    <DataGridTextColumn Header="Tên Dịch Vụ" Binding="{Binding TenDichVu}" Width="*"/>
                    <DataGridTextColumn Header="Đơn Giá" Binding="{Binding DonGia, StringFormat=N0}" Width="120"/>
                    <DataGridTextColumn Header="Đơn Vị" Binding="{Binding DonViTinh}" Width="100"/>
                    <DataGridTextColumn Header="Mô Tả" Binding="{Binding MoTa}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Hoạt Động" Binding="{Binding TrangThai}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Tổng số dịch vụ: " FontWeight="Bold" FontSize="14"/>
                <TextBlock x:Name="txt_TongSo" Text="0" FontWeight="Bold" 
                          FontSize="14" Foreground="#9C27B0" Margin="5,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
