<Window x:Class="LePhungHa_8991.View.QuanLyKhachHang"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LePhungHa_8991.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Quản Lý Khách Hàng" Height="700" Width="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="QUẢN LÝ KHÁCH HÀNG" FontSize="28" FontWeight="Bold" 
                   Foreground="#4CAF50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Label Content="Mã Khách Hàng:" FontWeight="Bold"/>
                    <TextBox x:Name="txt_MaKH" Height="35" Padding="5" FontSize="14"/>
                    
                    <Label Content="Họ Tên:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBox x:Name="txt_HoTen" Height="35" Padding="5" FontSize="14"/>
                    
                    <Label Content="CMND:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBox x:Name="txt_CMND" Height="35" Padding="5" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0">
                    <Label Content="Số Điện Thoại:" FontWeight="Bold"/>
                    <TextBox x:Name="txt_SoDienThoai" Height="35" Padding="5" FontSize="14"/>
                    
                    <Label Content="Email:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBox x:Name="txt_Email" Height="35" Padding="5" FontSize="14"/>
                    
                    <Label Content="Địa Chỉ:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBox x:Name="txt_DiaChi" Height="35" Padding="5" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="10,0,0,0">
                    <Label Content="Ngày Sinh:" FontWeight="Bold"/>
                    <DatePicker x:Name="dp_NgaySinh" Height="35" FontSize="14" 
                               SelectedDateChanged="dp_NgaySinh_SelectedDateChanged"/>
                    
                    <Label Content="Giới Tính:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <ComboBox x:Name="cbo_GioiTinh" Height="35" FontSize="14">
                        <ComboBoxItem Content="Nam"/>
                        <ComboBoxItem Content="Nữ"/>
                        <ComboBoxItem Content="Khác"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.ColumnSpan="3" Orientation="Horizontal" 
                           HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Thêm" Click="Them_Click" Width="100" Height="40" 
                           Margin="5" Background="#4CAF50" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Sửa" Click="Sua_Click" Width="100" Height="40" 
                           Margin="5" Background="#2196F3" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Xóa" Click="Xoa_Click" Width="100" Height="40" 
                           Margin="5" Background="#F44336" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Làm Mới" Click="LamMoi_Click" Width="100" Height="40" 
                           Margin="5" Background="#FF9800" Foreground="White" 
                           FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txt_TimKiem" Width="300" Height="35" 
                        Padding="5" FontSize="14" 
                        Text="Tìm kiếm theo tên, CMND, SĐT..."/>
                <Button Content="🔍 Tìm Kiếm" Click="TimKiem_Click" Width="120" Height="35" 
                       Margin="10,0,0,0" Background="#9C27B0" Foreground="White" 
                       FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>

            <DataGrid x:Name="dgv_KhachHang" Grid.Row="1" 
                     AutoGenerateColumns="False" 
                     IsReadOnly="True"
                     SelectionMode="Single"
                     SelectionChanged="dgv_KhachHang_SelectionChanged"
                     CanUserAddRows="False"
                     Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã KH" Binding="{Binding MaKH}" Width="80"/>
                    <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*"/>
                    <DataGridTextColumn Header="CMND" Binding="{Binding CMND}" Width="120"/>
                    <DataGridTextColumn Header="SĐT" Binding="{Binding SoDienThoai}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Địa Chỉ" Binding="{Binding DiaChi}" Width="*"/>
                    <DataGridTextColumn Header="Ngày Sinh" Binding="{Binding NgaySinh, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Giới Tính" Binding="{Binding GioiTinh}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Tổng số khách hàng: " FontWeight="Bold" FontSize="14"/>
                <TextBlock x:Name="txt_TongSo" Text="0" FontWeight="Bold" 
                          FontSize="14" Foreground="#4CAF50" Margin="5,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
